[package]
name = "veilvoice-conversation"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
authors.workspace = true
repository.workspace = true
description = "Several speakers in one recording: who spoke when, a distinct voice for each, names, and subtitles."

[dependencies]
veilvoice-core = { path = "../veilvoice-core" }
# For `privatefile`. A plan holds every speaker's name and every word
# somebody typed, which is the same content as the subtitles it produces.
veilvoice-crypto = { path = "../veilvoice-crypto" }

[dev-dependencies]
tempfile = "3"
